# Wrapper: keeps `docker compose up` working from the repository root.
# Canonical definition: infrastructure/compose/docker-compose.yml
name: chaoslabs

include:
  - path: infrastructure/compose/docker-compose.yml
